# Done to increase the memory available to gradle.
org.gradle.jvmargs=-Xmx1G
org.gradle.parallel=true

# Fabric Properties
minecraft_version=26.1.2
loader_version=0.19.3
# Loom changed a lot for 26.1 (unobfuscated Loom, no more remapping).
# Check https://fabricmc.net/develop/ for the exact current Loom release
# and update this if the build fails to resolve the plugin.
loom_version=1.17-SNAPSHOT

# Mod Properties
mod_version=1.0.0
maven_group=com.blockradar
archives_base_name=blockradar

# Dependencies
# Check https://modrinth.com/mod/fabric-api/versions?g=26.1.2 for the latest
# build tagged for 26.1.2 if this one has been superseded.
fabric_version=0.155.2+26.1.2
